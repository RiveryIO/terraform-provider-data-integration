"""Example logicode step: a placeholder Python transform.

Replace this with real logic. The logicode runtime injects a `rivery` builtin
at execution time — reference it inside a function (not at module top level),
since it isn't available yet when the module is first imported.
"""


def run():
    print("logicode step ran")
