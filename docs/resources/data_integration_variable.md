---
page_title: "boomi_data_integration_variable Resource - boomi"
subcategory: ""
description: |-
  A Data Integration environment variable. Variables are an environment-scoped key/value collection; each key is managed as its own resource (writes merge, so sibling keys are left intact).
---

# boomi_data_integration_variable (Resource)

A Data Integration environment variable. Variables are an environment-scoped key/value collection; each key is managed as its own resource (writes merge, so sibling keys are left intact).

## Example Usage

```terraform
resource "boomi_data_integration_variable" "example" {
  key   = "snowflake_schema"
  value = "PUBLIC"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `key` (String) Variable key. Must not contain "." or "$". Changing it forces a new variable.
- `value` (String) Variable value.

### Optional

- `environment_id` (String) Environment this variable belongs to. Falls back to the provider-level environment_id. Changing it forces a new variable.

### Read-Only

- `id` (String) Resource id. Equals the variable key (variables are keyed by name).
