---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "boomi_data_integration_river_group Data Source - boomi"
subcategory: ""
description: |-
  Looks up an existing river group (folder) by name. Groups must be created in the Data Integration UI — the v1 API exposes read-only access. Use this data source to obtain a group's cross_id and pass it as group_id to data flow resources.
---

# boomi_data_integration_river_group (Data Source)

Looks up an existing river group (folder) by name. Groups must be created in the Data Integration UI — the v1 API exposes read-only access. Use this data source to obtain a group's cross_id and pass it as group_id to data flow resources.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Exact name of the group to look up.

### Optional

- `environment_id` (String) Environment ID. Falls back to the provider default.

### Read-Only

- `color` (String) Hex colour assigned to the group in the UI.
- `icon` (String) Icon slug assigned to the group in the UI.
- `id` (String) Cross ID of the group (use as group_id on data flow resources).
- `is_default` (Boolean) True if this is the environment's default group.
